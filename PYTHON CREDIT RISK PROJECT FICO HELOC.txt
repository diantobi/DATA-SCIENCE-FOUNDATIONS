###PERSONAL CREDIT RISK PROJECT IN PROGRESS

import pandas as pd
df = pd.read_csv("RiskData.csv")
df.head()
df.shape
df.info()
print(df.describe(include="all").T)
df.columns

### DATA AUDIT
print(df["Risk_Flag"].value_counts())
print(df["Risk_Flag"].value_counts(normalize=True) * 100)

### Missing Values
df.isnull().sum()
(df == ".").sum()

print(df["FICO_Score"].value_counts().head(20))
print(df["Debt_Ratio"].value_counts().head(20))
print(df["CB_Rev_Util"].value_counts().head(20))
print((df == ".").sum())

#### DATA CLEANING
df = df.replace(".", pd.NA)
df.isna().sum()

### More Data Audit
print(df.groupby("Risk_Flag")["Age"].mean())
print(df.groupby("Risk_Flag")["Income"].mean())
print(df.groupby("Risk_Flag")["Loan_Amt"].mean())
print(df.groupby("Risk_Flag")["Loan_Amt_Req"].mean())
print(df.groupby("Risk_Flag")["LTV"].mean())
print(df.groupby("Risk_Flag")["Prior_Custom_Score"].mean())
print(df.groupby("Risk_Flag")["Current_Custom_Score"].mean())
print(df.groupby("Risk_Flag")["Bank_Relationship"].value_counts(normalize=True))
